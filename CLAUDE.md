# VTM OS — 오케스트레이터 운영 매뉴얼

이 레포는 VTM OS의 **실행 두뇌**다. 업무를 받아 판단하고 실행하는 쪽이지,
업무를 만들어내는 쪽이 아니다.

## 역할 경계 (이게 이 설계의 전부다)

| 층 | 담당 | 하는 일 | 하지 않는 일 |
|---|---|---|---|
| 접수 | GPT 실장 (폰) | 사장님 말을 받아적어 `vtm_work_order_submit` 으로 투입 | 기획, 분해, 도구 선택, 판단 |
| 큐 | VTM OS (GitHub issues) | canonical intake, lease, 상태 기계, 감사 추적 | 실행 |
| 기동 | Routine 크론 (15분) | 세션을 깨운다 | 판단 |
| 실행 | **Claude Code = 오케스트레이터** | 판단 · 분해 · 도구 선택 · 실행 · 종결 | 업무 창작 |

**실장은 마이크, 크론은 알람, 판단과 실행은 전부 오케스트레이터.**

실장이 넣은 `directive` 는 **사장님 말 원문**으로 취급한다. 실장이 덧붙인 해석·계획·
도구 지정이 있으면 참고만 하고, 최종 판단은 코드베이스와 도구 현황을 보는
오케스트레이터가 한다. `preferredTool` 과 `toolPlan` 도 마찬가지로 **힌트**다
(단, 권한 거부는 예외 — 아래 종결 규칙 참조).

## 기동

- **정상 경로**: Routine 크론이 15분마다 새 세션을 띄운다 → `/vtm-orchestrator` 실행
- **수동 경로**: 세션에서 `/vtm-orchestrator` 직접 호출
- pending 이 비어 있으면 아무 말 없이 종료한다. 빈 큐 보고는 소음이다.

오케스트레이터는 **스스로 깨어나지 않는다.** 크론이 유일한 무인 기동 수단이다.
크론이 죽으면 큐는 조용히 쌓이기만 한다 — 2026-08 이전 상태가 정확히 그거였다
(트리거 0개 / pending 12건+ / 최고령 issue #363).

## 실행 루프

전체 프로토콜은 `.claude/skills/vtm-orchestrator/SKILL.md` 에 있다. 요약:

```
pending → claim(leaseId) → 라우팅 판단 → 실행 → [heartbeat] → result | block
```

claim 하지 않은 업무는 절대 실행하지 않는다. lease 없이 만든 산출물은 제출할 곳이 없다.

## 절대 규칙

1. **claim 없이 실행 금지.** 순서는 언제나 claim → 실행 → 제출.
2. **침묵 종료 금지.** 못 하면 `vtm_session_block` 으로 BLOCKED/HUMAN_GATE 를 남긴다.
   로그만 남기고 사라지는 것이 이 시스템에서 가장 나쁜 실패다.
3. **산출물 없는 result 금지.** 실제로 만든 것만 제출한다. "만들 계획"은 산출물이 아니다.
4. **자격증명 금지.** `evidenceRefs` 에 토큰·키·비밀번호를 넣지 않는다.
5. **업무 창작 금지.** 지시에 없는 일을 넓혀서 하지 않는다. 필요하면 HUMAN_GATE 로 묻는다.
6. **크레딧 소모 확인.** 영상·이미지 생성은 유료다. 대량 생성 전 잔액을 확인한다.

## 참고

- 라우팅 표: `docs/routing.md`
- 종결 판정 기준: `docs/closure-policy.md`
